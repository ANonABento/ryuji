# Bug B.5 — Plugin API hardening + voice plugin completion audit

Per memory: choomfie voice plugin had phases (streaming TTS, VAD, interruption). Audit + close any gaps. Also harden the plugin API surface.

## Context
- Repo: `/Users/bentomac/choomfie`
- Plugin architecture: `packages/<name>/index.ts` exports `Plugin` interface
- Use a dedicated worktree.

## Scope

### B.5A — Voice plugin completion audit
Per `docs/voice-optimization-roadmap.md` (assuming exists), check status of:
- Streaming TTS (sentence pipelining): implemented + working?
- Silero VAD adaptive endpointing: present?
- Interruption handling (300ms barge-in, generation IDs): present?
- Streaming STT (3s flush): present?
- Multi-speaker per-VAD pipelines (max 4 concurrent, LRU eviction): present?
- Silence priming on join: present?

For each phase: if implemented, verify with a quick code review + test. If missing, document scope of remaining work in `docs/voice-todos.md` (don't implement in this PR — just inventory).

### B.5B — Plugin API hardening
- Document the `Plugin` interface in `docs/plugin-api.md` (formal contract)
- Add tests for the supervisor/worker hot-reload boundary — verify a worker restart doesn't drop in-flight tool calls or DM messages
- Test plugin lifecycle: init → onMessage hooks → destroy
- Verify the McpProxy duck-types correctly (worker sends tool calls; supervisor relays without modification)
- Add a `bun packages/core/scripts/plugin-smoke-test.ts` that loads each plugin, fires a synthetic message, verifies no crash

### B.5C — Document + harden
- `docs/plugin-api.md` — full Plugin interface with examples
- Add a "Common pitfalls" section: SSR safety not relevant here, but note: don't import worker-only code from packages/shared, etc.

## Acceptance criteria
- Voice plugin status documented in `docs/voice-todos.md`
- Plugin API documented in `docs/plugin-api.md`
- Smoke test script exists and runs
- Hot-reload behavior tested (at minimum: restart the worker, verify supervisor + active DM session survive)
- No code changes to plugin behavior beyond the smoke test infrastructure

## Out of scope
- Don't implement voice plugin gaps in this PR — just document
- Don't refactor plugin loader
- Don't merge

## Commit + PR
- `chore(plugins): hardening audit + Plugin API docs + smoke tests`
- **NO AI attribution footer**.
- Do NOT merge.

## Context
- Workspace: Choomfie
- Branch: bentoya/bug-b-5-plugin-api-hardening-voice-plugin-completi
- Working dir: /Users/bentomac/choomfie/.worktrees/bentoya-2121f54f-fe45-4813-8d73-8964d755d7e2
