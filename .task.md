# Webhook integrations — incoming webhooks per channel

Add HTTP endpoint /webhook/&lt;token&gt; that posts to a configured Discord channel. Token map in SQLite. /webhook create generates new token + URL, /webhook list, /webhook revoke. Owner-only config. Acceptance: webhook URL posts to channel, tokens revocable, bun test passes.

## Context
- Workspace: Choomfie
- Branch: bentoya/webhook-integrations-incoming-webhooks-per-channel
- Working dir: /Users/bentomac/choomfie/.worktrees/bentoya-597c873e-8ff6-4412-ab04-740d27fe2a94
