# IMPROVE-04: Interview Prep — live feedback during mock

**Status: BLOCKED — repo misroute. No implementation in this worktree.**

## Why this task cannot be implemented here

This worktree is on the **Choomfie** monorepo (Discord MCP plugin):
- Path: `/Users/bentomac/choomfie/.worktrees/bentoya-37622ae7-2a6a-446e-8176-196647ace548`
- Layout: `packages/core` (Discord bridge), `packages/shared`, `plugins/{voice,browser,tutor,socials}`
- No `apps/web/`, no Next.js, no Interview Prep feature.

The spec and target files reference paths that exist only in **get-me-job** (Next.js / `@slothing/web`):
- Spec: `/Users/bentomac/get-me-job/docs/audit/feature-specs/2026-05-10-improve-04-interview-live-feedback.md` ✅ exists
- Target dirs: `/Users/bentomac/get-me-job/apps/web/src/lib/interview/`, `/Users/bentomac/get-me-job/apps/web/src/components/interview/` ✅ exist
- Choomfie equivalents: ❌ do not exist (no `apps/web/`).

Running this task here would either (a) pollute Choomfie with a phantom `apps/web/` tree, or (b) fail trivially. Either is worse than parking the task and letting an operator re-route.

This is the **second time** an agent has hit this wall on this branch. The prior run is preserved in commit `3ef8167` (auto-committed by bento-ya after the agent exited 0 without committing). That commit contains the prior agent's full implementation plan written against the get-me-job paths — reuse it when re-running.

## What the operator should do

Pick one:

1. **Re-route the task in bento-ya** to a fresh worktree under `/Users/bentomac/get-me-job/.worktrees/...` from the right base branch. Keep the branch name `bentoya/improve-04-interview-prep-live-feedback-during-moc`.
2. **Run manually** in the get-me-job repo:
   ```
   cd /Users/bentomac/get-me-job
   git checkout -b bentoya/improve-04-interview-prep-live-feedback-during-moc
   # then implement against the spec
   ```
3. **Cancel** this task in the bento-ya board if it's no longer needed.

## Original task brief (preserved for re-routing)

Implement spec at `docs/audit/feature-specs/2026-05-10-improve-04-interview-live-feedback.md` (in get-me-job).

Interview Prep transcribes user answers but gives no feedback. Add a heuristic coaching layer with per-answer scorecards + end-of-session aggregate.

Metrics:
1. Filler word count (um, like, you know, etc)
2. STAR coverage (keyword-based detection of Situation/Task/Action/Result)
3. Quantification count (numbers/metrics in answer)
4. Length vs target (60-90s behavioral, 90-180s technical)
5. Pace (words per minute, target 130-150)

New files (paths relative to `/Users/bentomac/get-me-job/`):
- `apps/web/src/lib/interview/feedback.ts` — analyze transcript → scorecard
- `apps/web/src/lib/interview/star-detector.ts` — keyword-based STAR detection
- `apps/web/src/lib/interview/filler-words.ts` — count fillers
- `apps/web/src/components/interview/answer-feedback-card.tsx` — per-answer scorecard
- `apps/web/src/components/interview/session-feedback-summary.tsx` — end-of-session aggregate

Modify:
- `apps/web/src/components/interview/interview-active-session.tsx` — show feedback after each answer

Acceptance:
- Scorecard appears below each answered question
- All 5 metrics visible with color-coded thresholds (green/yellow/red)
- Top suggestion text (single most actionable feedback)
- End-of-session aggregate report
- Test fixtures: 3 sample answers (strong/medium/weak) → assert correct ratings
- type-check + lint clean

Out of scope: LLM-based feedback, body language webcam, real-time during-answer indicators.

## Context
- Workspace: Choomfie (WRONG — should be get-me-job)
- Branch: bentoya/improve-04-interview-prep-live-feedback-during-moc
- Working dir (this worktree): /Users/bentomac/choomfie/.worktrees/bentoya-37622ae7-2a6a-446e-8176-196647ace548
- Correct working dir: /Users/bentomac/get-me-job (or a worktree under it)
