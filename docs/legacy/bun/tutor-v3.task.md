# Tutor v3 Task Archive

This mid-flight plan has been reconciled into the root `.task.md` finalization
plan.

Use `.task.md` as the current source of truth for Tutor v3 completion,
validation, and acceptance criteria. The older version of this document
described gaps that are now implemented, including active-lesson
`furiganaLevel` prompt wiring and the catalog/profile regression tests.
