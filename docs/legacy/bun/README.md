# Legacy Bun Docs

These docs describe the previous Choomfie implementation under `legacy/bun/`.

They are still useful while porting behavior into Hermes, but they are no longer the default runtime documentation.

